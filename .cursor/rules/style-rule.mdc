---
alwaysApply: true
---
1. Code Style
🔹 Backend (Spring Boot / Java 17+)

Indentation: 4 spaces, no tabs.

Naming conventions:

Classes → PascalCase

Methods & variables → camelCase

Constants → UPPER_SNAKE_CASE

Imports: auto-organized (no unused imports); use import specific.* not wildcard.

Annotations order: @RestController / @Service on top, followed by @RequiredArgsConstructor, etc.

Braces: always on new line (K&R style).

Line length: max 120 chars.

Lombok: allowed, but annotate consistently (@Getter, @Setter, @Builder, @Slf4j).

Javadoc: mandatory for public classes and methods.

Formatting: enforce with spotless-maven-plugin or google-java-format.

🔹 Frontend (Next.js 16 / TypeScript)

Indentation: 2 spaces.

Naming conventions:

Components → PascalCase

Hooks/functions → camelCase

Interfaces/types → PascalCase

Constants/env vars → UPPER_SNAKE_CASE

Imports:

Group order: React → libs → utils → components → styles.

No relative path hell (../../..): use tsconfig.paths aliases (@/components/...).

Max line length: 100 chars.

Quotes: single ' preferred, double " only for JSON.

Semicolons: always required.

Auto-format: via Prettier + ESLint + eslint-config-next.

CSS: prefer CSS Modules or Tailwind, not inline style soup.

2. Code Principles
🔹 Common Architecture Rules

DRY: reusable logic → utils/services (shared between BE/FE when possible).

KISS: avoid over-abstraction, code should read like English.

SOLID: enforce for Spring components, DTOs, and service layers.

YAGNI: don’t write config, endpoint, or state until it’s needed.

Type-safety everywhere:

BE: use record, DTOs, and validation annotations (@NotNull, @Valid).

FE: full TypeScript typing (interface, type, zod for schema validation).

API Contracts: maintain with OpenAPI → auto-generate TypeScript client types using openapi-typescript.

Error Handling:

BE: custom @ControllerAdvice for global exceptions.

FE: toast/alert handling + clear fallback UI.

Security:

BE: enable Spring Security + CORS config.

FE: sanitize inputs, no direct cookie access.

3. Cursor Behavior Rules

Cursor (AI) must behave like a disciplined senior dev in a fullstack repo:

Do not auto-rewrite backend logic.

Preserve all annotations, Javadoc, and decorators.

Infer context: when editing frontend, keep Next.js 16 conventions (app/ directory, server components).

Always add/keep TypeScript types & prop interfaces.

When generating React components:

Must be function components.

Must use hooks instead of class lifecycle.

Must handle async with useEffect or server actions (Next 16 style).

Backend code generation:

Follow layered architecture: controller → service → repository.

Include DTO mapping if creating endpoints.

Tests:

BE → JUnit 5 + Mockito.

FE → Vitest or Jest + React Testing Library.

Never modify existing documentation or business comments.

Lint before commit: mvn spotless:apply (BE) + pnpm lint (FE).